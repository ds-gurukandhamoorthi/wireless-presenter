Use as `sudo ./events.py`

the program actkbd doesn't grab the keys, so it pollutes the current window we are in. This script is to solve this problem in view of using a bluetooth wirless controller.
